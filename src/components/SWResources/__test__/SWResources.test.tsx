import { render } from '@testing-library/react';
import SWResources from '..';

describe('SWResources', () => {
  test('renders without errors', () => {
    render(<SWResources />);
    // No error occurred during rendering
  });

});
